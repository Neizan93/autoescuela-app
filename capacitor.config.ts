import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.profedrive.app',
  appName: 'ProfeDrive',
  webDir: 'dist/autoescuela-app/browser'
};

export default config;
